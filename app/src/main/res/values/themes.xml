<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.SuperHero" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:statusBarColor">@color/bg_app</item>
         <item name="colorPrimary">@color/bg_app</item>
    </style>

    <style name="Theme.SuperHero" parent="Theme.AppCompat.DayNight.DarkActionBar" >
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/bg_app</item>

        <!-- Action bar color. -->
        <item name="colorPrimary">@color/bg_app</item>
    </style>

    <style name="AppSearchView" parent="" >
        <item name="android:textSize">26sp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:background">@drawable/bg_rounded</item>
    </style>

</resources>